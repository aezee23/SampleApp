<div class="list-group">
  <a href="#" class="list-group-item active">
   Missing <%= @date %> Data For the Following <%= pluralize(@users.count, "Church") %>
  </a>
  <div class="list-group">
  	<% @users.each do |t| %>
  	<%= link_to(t) do %>
  <li class="list-group-item list-group-item-danger"><%=t.name%> ---- <%= t.elder %></li>
  <%end%>
  <% end %>
</div>
</div>