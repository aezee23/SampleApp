<div class="list-group">
  <a href="#" class="list-group-item active">
   Missing <%= @date %> Data For the Following <%= pluralize(@users.count, "Church") %>
  </a>
  <div class="list-group">
  	<% @users.each do |t| %>
  <a href="#" class="list-group-item list-group-item-danger"><%=t.name%></a>
  <% end %>
</div>
</div>