<div class="list-group">
  <a href="#" class="list-group-item active">
   Missing <%= @date %> Data For the Following <%= pluralize(@users.count, "Church") %>
  </a>
  <div class="list-group">
  	<% @users.each do |t| %>
  
  <li><%= link_to "#{t.name} -- #{t.elder}", user_path(t), class: "list-group-item list-group-item-danger" %></li>
  <% end %>

</div>
</div>