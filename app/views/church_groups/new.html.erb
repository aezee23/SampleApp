<% provide(:title, 'Create New church Group') %>
<h1>New Church group</h1>

<div>
  <div>
    <%= form_for(@church_group) do |f| %>
 <div class="form-group">
    
      <%= f.label :name %>
      <%= f.text_field :name, placeholder: "Name" %>
</div>

      <%= f.submit "Create my account", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>